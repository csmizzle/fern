version: '3.8'
services:
  tika-server:
    image: apache/tika:latest-full
    ports:
      - "9998:9998"
    volumes:
      - "./config/tika-config.xml:/tika-config.xml"
    command:
      - -c /tika-config.xml
    restart: always
